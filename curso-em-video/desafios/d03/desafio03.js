function inicio() {
    var numero = parseInt(prompt('Digite um número inteiro qualquer:'));
    window.alert(`Antes do número ${numero}, temos número ${numero - 1}. \nDepois do número ${numero}, temos o número ${numero + 1}.`);
}